import { HttpShellDirective } from './http-shell.directive';

describe('HttpShellDirective', () => {
  it('should create an instance', () => {
    const directive = new HttpShellDirective();
    expect(directive).toBeTruthy();
  });
});
